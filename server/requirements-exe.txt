# EXE packaging dependencies
# Excludes heavy ML dependencies (sentence-transformers, PyTorch, streamlit)
# Embedding will use hash-based fallback when sentence-transformers is unavailable

# Web framework
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18

# Database
sqlalchemy>=2.0.0

# Vector store
chromadb>=1.0.0

# LLM API (DeepSeek uses OpenAI-compatible API)
openai>=1.80.0

# Document parsing
pymupdf>=1.25.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.26.0
